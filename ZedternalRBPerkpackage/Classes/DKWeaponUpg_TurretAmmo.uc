class DKWeaponUpg_TurretAmmo extends DKUpgrade_Weapon
	abstract
	config(ZedternalUnlimited);

var config float AmmoBonus;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 2)
	{
		default.AmmoBonus = 0.5f;
		default.MODEVERSION = 2;
		static.StaticSaveConfig();
	}
}

static function bool IsUpgradeCompatible(class<KFWeapon> KFW)
{
	if (class<KFWeap_AutoTurret>(KFW) != None)
		return True;
	if (class<KFWeap_HRG_Warthog>(KFW) != None)
		return True;
	if (WMTurret_Interface(KFW) != None)
		return True;
	return False;
}

static simulated function ModifySpareAmmoAmount(out int InSpareAmmo, int DefaultSpareAmmo, int upgLevel, KFWeapon KFW, optional const out STraderItem TraderItem, optional bool bSecondary=False)
{
	local float Val;
	Val = default.AmmoBonus;
	if (Val <= 0.f) Val = 0.5f;
	InSpareAmmo += Round(float(DefaultSpareAmmo) * Val * upgLevel);
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKWeaponUpg_TurretAmmo]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKWeaponUpg_TurretAmmo"

	UpgradeName="Battery Pack"
	UpgradeDescription(0)="Increases your <font color=\"#15d7fa\">turret ammo capacity</font> by <font color=\"#77d914\">+%x%%</font> per level."
	WeaponBonus=(baseValue=0, incValue=50, maxValue=-1)
	Name="Default__DKWeaponUpg_TurretAmmo"
}
