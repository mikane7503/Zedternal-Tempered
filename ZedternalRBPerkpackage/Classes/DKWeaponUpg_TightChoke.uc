class DKWeaponUpg_TightChoke extends DKUpgrade_Weapon
	abstract
	config(ZedternalUnlimited);

var config float SpreadReduction;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 2)
	{
		default.SpreadReduction = 0.2f;
		default.MODEVERSION = 2;
		static.StaticSaveConfig();
	}
}

static function bool IsUpgradeCompatible(class<KFWeapon> KFW)
{
	if (KFW.default.NumPellets[KFW.const.DEFAULT_FIREMODE] > 1)
		return True;
	if (KFW.default.NumPellets[KFW.const.ALTFIRE_FIREMODE] > 1)
		return True;
	if (KFW.default.NumPellets[KFW.const.CUSTOM_FIREMODE] > 1)
		return True;
	return False;
}

static simulated function ModifyTightChoke(out float InTight, float DefaultTight, int upgLevel, KFWeapon KFW, KFPawn OwnerPawn)
{
	local float Val;
	Val = default.SpreadReduction;
	if (Val <= 0.f) Val = 0.2f;
	InTight -= Val * upgLevel;
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKWeaponUpg_TightChoke]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKWeaponUpg_TightChoke"

	UpgradeName="Laser Focus"
	UpgradeDescription(0)="Reduces <font color=\"#15d7fa\">shotgun spread</font> by <font color=\"#77d914\">%x%%</font> per level. (multi-pellet weapons)"
	WeaponBonus=(baseValue=0, incValue=20, maxValue=-1)
	Name="Default__DKWeaponUpg_TightChoke"
}
