class DKUpgrade_Skill_Marathon extends DKUpgrade_Skill
	config(ZedternalUnlimited);

var config array<float> FireRate, SpecialRate;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.FireRate[0] = 0.2f;
		default.FireRate[1] = 0.4f;
		default.SpecialRate[0] = 0.4f;
		default.SpecialRate[1] = 0.8f;

		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static simulated function bool IsUnAffectedByZedTime(int upgLevel, KFPawn OwnerPawn)
{
	return True;
}

static simulated function GetZedTimeModifier(out float InModifier, int upgLevel, KFWeapon KFW)
{
	local name StateName;

	if (KFW != None)
	{
		StateName = KFW.GetStateName();
		if (class'ZedternalReborn.WMWeaponStates'.static.IsWeaponAttackState(StateName))
		{
			if (upgLevel > 0)
			{
				if (KFWeap_MeleeBase(KFW) != None || KFW.default.MagazineCapacity[0] > 4)
				{
					if (upgLevel <= default.FireRate.Length)
						InModifier += default.FireRate[upgLevel - 1];
				}
				else
				{
					if (upgLevel <= default.SpecialRate.Length)
						InModifier += default.SpecialRate[upgLevel - 1];
				}
			}
		}
	}
}

static simulated function RevertUpgradeChanges(Pawn OwnerPawn)
{
	if (KFPawn_Human(OwnerPawn) != None)
			KFPawn_Human(OwnerPawn).bMovesFastInZedTime = False;
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKUpgrade_Skill_Marathon]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKUpgrade_Skill_Marathon"

    UpgradeName="Marathon"
	upgradeDescription(0)="During Zed Time, increase your<font color=\"#ff3399\">Movement Speed</font> by <font color=\"#66cc00\">40%</font>."
    upgradeDescription(1)="During Zed Time, increase your<font color=\"#ff3399\">Movement Speed</font> by <font color=\"#66cc00\">80%</font>."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_Marathon'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_Marathon_Deluxe'
    Name="Default__DKUpgrade_Skill_Marathon"
}