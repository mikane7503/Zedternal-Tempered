class DKWeaponUpg_TurretLimit extends DKUpgrade_Weapon
	abstract
	config(ZedternalUnlimited);

var config int ExtraTurrets;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 2)
	{
		default.ExtraTurrets = 1;
		default.MODEVERSION = 2;
		static.StaticSaveConfig();
	}
}

static function bool IsUpgradeCompatible(class<KFWeapon> KFW)
{
	if (class<KFWeap_AutoTurret>(KFW) != None)
		return True;
	if (class<KFWeap_HRG_Warthog>(KFW) != None)
		return True;
	if (WMTurret_Interface(KFW) != None)
		return True;
	return False;
}

static simulated function ModifyMaxDeployed(out int CurrentMaxDeployed, int DefaultMaxDeployed, int upgLevel, KFWeapon KFW)
{
	local int Val;
	Val = default.ExtraTurrets;
	if (Val <= 0) Val = 1;
	CurrentMaxDeployed += Val * upgLevel;
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKWeaponUpg_TurretLimit]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKWeaponUpg_TurretLimit"

	UpgradeName="Assembly Line"
	UpgradeDescription(0)="Allows deploying <font color=\"#77d914\">+%x</font> additional <font color=\"#15d7fa\">turret</font> per level."
	WeaponBonus=(baseValue=0, incValue=1, maxValue=-1)
	Name="Default__DKWeaponUpg_TurretLimit"
}
