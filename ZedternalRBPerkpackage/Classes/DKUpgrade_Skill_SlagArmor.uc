class DKUpgrade_Skill_SlagArmor extends DKUpgrade_Skill
	config(ZedternalUnlimited);

// Slag Armor: Reduced damage taken from fire, explosive, and toxic sources
// Normal: -30% damage | Deluxe: -70% damage

var config array<float> Damage;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.Damage[0] = 0.3f;
		default.Damage[1] = 0.7f;

		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static function ModifyDamageTaken(out int InDamage, int DefaultDamage, int upgLevel, KFPawn OwnerPawn, optional class<DamageType> DamageType, optional Controller InstigatedBy, optional KFWeapon MyKFW)
{
	if (DamageType != None && (ClassIsChildOf(DamageType, class'KFDT_Fire') || ClassIsChildOf(DamageType, class'KFDT_Explosive') || ClassIsChildOf(DamageType, class'KFDT_Toxic')))
		InDamage -= Round(float(DefaultDamage) * default.Damage[upgLevel - 1]);
}

defaultproperties
{

    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKUpgrade_Skill_SlagArmor]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKUpgrade_Skill_SlagArmor"

	UpgradeName="Slag Armor"
	UpgradeDescription(0)="Take <font color=\"#e85009\">30%</font> less damage from <font color=\"#e80948\">fire, explosive</font>, and toxic sources."
	UpgradeDescription(1)="Take <font color=\"#e85009\">70%</font> less damage from <font color=\"#e80948\">fire, explosive</font>, and toxic sources."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_SlagArmor'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_SlagArmor_Deluxe'

	Name="Default__DKUpgrade_Skill_SlagArmor"
}
