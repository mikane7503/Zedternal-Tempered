class DKUpgrade_Skill_BlackIce extends DKUpgrade_Skill
	config(ZedternalUnlimited);

var config array<float> Chance;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.Chance[0] = 0.15f;
		default.Chance[1] = 0.30f;

		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static function ModifyDamageGiven(out int InDamage, int DefaultDamage, int upgLevel, optional Actor DamageCauser, optional KFPawn_Monster MyKFPM, optional KFPlayerController DamageInstigator, optional class<KFDamageType> DamageType, optional int HitZoneIdx, optional KFWeapon MyKFW)
{
	local rotator Rot;
	local vector Loc;

	if((((((MyKFPM != none) && MyKFPM.IsAliveAndWell()) && (MyKFPM.Health - InDamage) <= 0) && DamageInstigator != none) && (FRand() <= (default.Chance[upgLevel - 1]))))
	 {	
		Rot = rotator(MyKFPM.Velocity);
		Loc = MyKFPM.Location;
		Loc.Z -= MyKFPM.GetCollisionHeight();
		Rot.Pitch = 0;
		if (upgLevel == 1)
			DamageInstigator.Spawn(class'ZedternalRBPerkpackage.DKProj_BlackIce', DamageInstigator, , Loc, Rot, , True);
		else
			DamageInstigator.Spawn(class'ZedternalRBPerkpackage.DKProj_BlackIce_Deluxe', DamageInstigator, , Loc, Rot, , True);
     }
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKUpgrade_Skill_BlackIce]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKUpgrade_Skill_BlackIce"

    UpgradeName="Black Ice"
	upgradeDescription(0)="Killing a ZED has a <font color=\"#15d7fa\">15% Chance</font> to cover the ground under them in <font color=\"#0bf1f1\">Black Ice</font>, slowing down all ZEDs walking over it."
	upgradeDescription(1)="Killing a ZED has a <font color=\"#15d7fa\">30% Chance</font> to cover the ground under them in <font color=\"#0bf1f1\">Black Ice</font>, slowing down all ZEDs walking over it."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_BlackIce'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_BlackIce_Deluxe'

	Name="Default__DKUpgrade_Skill_BlackIce"
}