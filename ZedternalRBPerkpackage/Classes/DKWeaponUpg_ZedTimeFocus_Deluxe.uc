// Zed Time Focus weapon upgrade — Deluxe tier
// +8% damage per level during Zed Time.
// L1=8%, L5=40%, L10=80%
class DKWeaponUpg_ZedTimeFocus_Deluxe extends DKWeaponUpg_HollowBase
	abstract
	config(ZedternalUnlimited);

var config float ZedTimeBonus;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.ZedTimeBonus = 0.08f;
		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}

static function bool IsUpgradeCompatible(class<KFWeapon> KFW)
{
	return True;
}

static function ModifyDamageGiven(out int InDamage, int DefaultDamage, int upgLevel, optional Actor DamageCauser, optional KFPawn_Monster MyKFPM, optional KFPlayerController DamageInstigator, optional class<KFDamageType> DamageType, optional int HitZoneIdx, optional KFWeapon MyKFW)
{
	if (DamageInstigator == None || DamageInstigator.WorldInfo == None)
		return;

	if (DamageInstigator.WorldInfo.TimeDilation >= 0.5f)
		return;

	InDamage += Round(float(DefaultDamage) * default.ZedTimeBonus * upgLevel);
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKWeaponUpg_ZedTimeFocus_Deluxe]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKWeaponUpg_ZedTimeFocus_Deluxe"

	UpgradeName="Zed Time Focus (Deluxe)"
	UpgradeDescription(0)="Deals <font color=\"#00CCFF\">+%x%%</font> additional damage during <font color=\"#6B0099\">Zed Time</font> per level."
	WeaponBonus=(baseValue=0, incValue=8, maxValue=-1)
	RequiredSkillClassPath="ZedternalRBPerkpackage.DKUpgrade_Skill_ZedTimeFocus"
	bRequiresDeluxe=True
	Name="Default__DKWeaponUpg_ZedTimeFocus_Deluxe"
}
