class DKUpgrade_Skill_Flashpoint extends DKUpgrade_Skill;

static function ModifyDamageTaken(out int InDamage, int DefaultDamage, int upgLevel, KFPawn OwnerPawn, optional class<DamageType> DamageType, optional Controller InstigatedBy, optional KFWeapon MyKFW)
{
	local DKUpgrade_Skill_Flashpoint_Helper UPG;

	if (InDamage > 0)
	{
		UPG = GetHelper(OwnerPawn, upgLevel);
		if (UPG != None && UPG.bReady)
			UPG.FireNova();
	}
}

static function DKUpgrade_Skill_Flashpoint_Helper GetHelper(KFPawn OwnerPawn, int upgLevel)
{
	local DKUpgrade_Skill_Flashpoint_Helper UPG;

	if (KFPawn_Human(OwnerPawn) != None)
	{
		foreach OwnerPawn.ChildActors(class'DKUpgrade_Skill_Flashpoint_Helper', UPG)
		{
			return UPG;
		}

		//Should have one
		UPG = OwnerPawn.Spawn(class'DKUpgrade_Skill_Flashpoint_Helper', OwnerPawn);
		UPG.bDeluxe = (upgLevel > 1);
	}

	return UPG;
}

static simulated function DeleteHelperClass(Pawn OwnerPawn)
{
	local DKUpgrade_Skill_Flashpoint_Helper UPG;

	if (OwnerPawn != None)
	{
		foreach OwnerPawn.ChildActors(class'DKUpgrade_Skill_Flashpoint_Helper', UPG)
		{
			UPG.Destroy();
		}
	}
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKUpgrade_Skill_Flashpoint]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKUpgrade_Skill_Flashpoint"

	UpgradeName="Flashpoint"
	upgradeDescription(0)="When you take damage, release a <font color=\"#e80948\">Fire Nova</font> that ignites all nearby ZEDs. <font color=\"#e85009\">30 second</font> cooldown."
	upgradeDescription(1)="When you take damage, release a massive <font color=\"#e80948\">Fire Nova</font> that ignites all nearby ZEDs. <font color=\"#e85009\">20 second</font> cooldown."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_Flashpoint'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_Flashpoint_Deluxe'

	Name="Default__DKUpgrade_Skill_Flashpoint"
}
