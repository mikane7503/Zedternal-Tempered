// Execute weapon upgrade — Deluxe tier
// +8% bonus damage per level to zeds below 35% HP.
// L1=8%, L5=40%, L10=80%
// Requires skill: DKUpgrade_Skill_Execute (Deluxe)
class DKWeaponUpg_Execute_Deluxe extends DKWeaponUpg_HollowBase
	abstract
	config(ZedternalUnlimited);

var config float DamageBonus;
var config float ExecuteThreshold;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.DamageBonus = 0.08f;
		default.ExecuteThreshold = 0.35f;
		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}

static function bool IsUpgradeCompatible(class<KFWeapon> KFW)
{
	return True;
}

static function ModifyDamageGiven(out int InDamage, int DefaultDamage, int upgLevel, optional Actor DamageCauser, optional KFPawn_Monster MyKFPM, optional KFPlayerController DamageInstigator, optional class<KFDamageType> DamageType, optional int HitZoneIdx, optional KFWeapon MyKFW)
{
	if (MyKFPM == None || MyKFPM.HealthMax <= 0)
		return;

	if (float(MyKFPM.Health) / float(MyKFPM.HealthMax) > default.ExecuteThreshold)
		return;

	InDamage += Round(float(DefaultDamage) * default.DamageBonus * upgLevel);
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKWeaponUpg_Execute_Deluxe]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKWeaponUpg_Execute_Deluxe"

	UpgradeName="Execute (Deluxe)"
	UpgradeDescription(0)="Deals <font color=\"#00CCFF\">+%x%%</font> additional damage to enemies below <font color=\"#6B0099\">35% HP</font> per level."
	WeaponBonus=(baseValue=0, incValue=8, maxValue=-1)
	RequiredSkillClassPath="ZedternalRBPerkpackage.DKUpgrade_Skill_Execute"
	bRequiresDeluxe=True
	Name="Default__DKWeaponUpg_Execute_Deluxe"
}
