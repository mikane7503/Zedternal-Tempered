class DKUpgrade_Weapon_DualArmory extends DKUpgrade_Weapon
    abstract
    hidecategories(Navigation,Movement,Collision,Navigation,Movement,Collision);

var float SpareAmmo, MagSize;

static function bool IsUpgradeCompatible(Class<KFWeapon> KFD)
{
    return true;
    //return ReturnValue;    
}

static simulated function ModifySpareAmmoAmount(out int InSpareAmmo, int DefaultSpareAmmo, int upgLevel, KFWeapon KFW, const optional out STraderItem TraderItem, optional bool bSecondary)
{
    bSecondary = false;
    if(!bSecondary)
    {
       InSpareAmmo += Round((float(DefaultSpareAmmo) * default.SpareAmmo) * float(upgLevel));
    }    
}

static simulated function ModifyMagSizeAndNumber(out int InMagazineCapacity, int DefaultMagazineCapacity, int upgLevel, KFWeapon KFW, optional array< Class<KFPerk> > WeaponPerkClass, optional bool bSecondary, optional name WeaponClassName)
{
    bSecondary = false;
    InMagazineCapacity += Round((float(DefaultMagazineCapacity) * default.MagSize) * float(upgLevel));
    //return;    
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKUpgrade_Weapon_DualArmory]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKUpgrade_Weapon_DualArmory"

    SpareAmmo=0.05000000
	MagSize=0.0500000
    WeaponBonus=(incValue=5,MaxValue=-1)
	UpgradeName="Dual Armory"
    UpgradeDescription(0)="Increases your <font color=\"#15d7fa\">spare ammunition</font> and <font color=\"#15d7fa\">magazine size</font> by <font color=\"#77d914\">+%x%%</font> per level."
}