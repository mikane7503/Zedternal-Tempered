// Void Sustain weapon upgrade — Deluxe tier
// +3.5% chance per level on kill to refill magazine.
// L1=3.5%, L5=17.5%, L10=35%
class DKWeaponUpg_VoidSustain_Deluxe extends DKWeaponUpg_HollowBase
	abstract
	config(ZedternalUnlimited);

var config float RefillChancePerLevel;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.RefillChancePerLevel = 0.035f;
		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}

static function bool IsUpgradeCompatible(class<KFWeapon> KFW)
{
	return True;
}

static function ModifyDamageGiven(out int InDamage, int DefaultDamage, int upgLevel, optional Actor DamageCauser, optional KFPawn_Monster MyKFPM, optional KFPlayerController DamageInstigator, optional class<KFDamageType> DamageType, optional int HitZoneIdx, optional KFWeapon MyKFW)
{
	local float Chance;

	if (MyKFW == None || MyKFPM == None)
		return;

	if (InDamage < MyKFPM.Health)
		return;

	Chance = default.RefillChancePerLevel * upgLevel;
	if (FRand() >= Chance)
		return;

	if (MyKFW.MagazineCapacity[0] > 0)
		MyKFW.AmmoCount[0] = MyKFW.MagazineCapacity[0];
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKWeaponUpg_VoidSustain_Deluxe]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKWeaponUpg_VoidSustain_Deluxe"

	UpgradeName="Void Sustain (Deluxe)"
	UpgradeDescription(0)="<font color=\"#00CCFF\">%x%%</font> chance on kill to <font color=\"#6B0099\">instantly refill your magazine</font> per level."
	WeaponBonus=(baseValue=0, incValue=3, maxValue=-1)
	RequiredSkillClassPath="ZedternalRBPerkpackage.DKUpgrade_Skill_VoidSustain"
	bRequiresDeluxe=True
	Name="Default__DKWeaponUpg_VoidSustain_Deluxe"
}
