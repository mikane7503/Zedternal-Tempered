class DKUpgrade_Skill_RegenerativeRounds extends DKUpgrade_Skill
	config(ZedternalUnlimited);

var config array<float> Ammo;
var config array<float> SpareAmmo;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.Ammo[0] = 1.00f;
		default.Ammo[1] = 2.00f;
		default.SpareAmmo[0] = 1.00f;
		default.SpareAmmo[1] = 2.00f;

		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static simulated function InitiateWeapon(int upgLevel, KFWeapon KFW, KFPawn OwnerPawn)
{
	local byte i;

	if (KFW != None)
	{
		for (i = 0; i <= 1; ++i)
		{
			KFW.AmmoPickupScale[i] = KFW.default.AmmoPickupScale[i] * default.Ammo[upgLevel - 1];
		}
	}

	if (KFWeap_Thrown_C4(KFW) != None)
		KFW.AmmoPickupScale[0] = FCeil(KFW.AmmoPickupScale[0]);
}

static simulated function ModifySpareAmmoAmountPassive(out float spareAmmoFactor, int upgLevel)
{
	spareAmmoFactor += default.SpareAmmo[upgLevel - 1];
}

defaultproperties
{    
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKUpgrade_Skill_RegenerativeRounds]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKUpgrade_Skill_RegenerativeRounds"

	UpgradeName="RegenerativeRounds"
	upgradeDescription(0)="Gain <font color=\"#0bf1f1\">100% Spare Ammunition</font> and Ammunition Pickups contain <font color=\"#0bf1f1\">100% more</font>."
	upgradeDescription(1)="Gain <font color=\"#0bf1f1\">200% Spare Ammunition</font> and Ammunition Pickups contain <font color=\"#0bf1f1\">200% more</font>."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_RegenerativeRounds'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_RegenerativeRounds_Deluxe'
    Name="Default__DKUpgrade_Skill_RegenerativeRounds"
}