class DKUpgrade_Skill_HODLing extends DKUpgrade_Skill
	config(ZedternalUnlimited);

var config array<float> HODLDuration;
var config float InterestRate;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.HODLDuration[0] = 600.0f;
		default.HODLDuration[1] = 450.0f;
		default.InterestRate = 0.50f;

		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static simulated function InitiateWeapon(int upgLevel, KFWeapon KFW, KFPawn OwnerPawn)
{
    local DKUpgrade_Skill_HODLing_Helper UPG;
    local bool bFound;

    if (KFPawn_Human(OwnerPawn) != None && OwnerPawn.Role == Role_Authority)
    {
        bFound = False;
        foreach OwnerPawn.ChildActors(class'DKUpgrade_Skill_HODLing_Helper', UPG)
        {
            bFound = True;
            break;
        }

        if (!bFound)
        {
            UPG = OwnerPawn.Spawn(class'DKUpgrade_Skill_HODLing_Helper', OwnerPawn);
            UPG.bDeluxe = (upgLevel > 1);
            UPG.UpgradeLevel = upgLevel;
        }
    }
}

static simulated function DeleteHelperClass(Pawn OwnerPawn)
{
    local DKUpgrade_Skill_HODLing_Helper UPG;

    if (OwnerPawn != None)
    {
        foreach OwnerPawn.ChildActors(class'DKUpgrade_Skill_HODLing_Helper', UPG)
        {
            UPG.Destroy();
        }
    }
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKUpgrade_Skill_HODLing]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKUpgrade_Skill_HODLing"

    UpgradeName="HODLing"
    
    upgradeDescription(0)="Not spending dosh for <font color=\"#ffaa33\">600 seconds</font> grants <font color=\"#00ff00\">+50% interest</font> on current amount (timer paused between waves)."
    upgradeDescription(1)="Not spending dosh for <font color=\"#ffaa33\">450 seconds</font> grants <font color=\"#00ff00\">+50% interest</font> on current amount (timer paused between waves)."
    
    UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_HODLing'
    UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_HODLing_Deluxe'

    Name="Default__DKUpgrade_Skill_HODLing"
}