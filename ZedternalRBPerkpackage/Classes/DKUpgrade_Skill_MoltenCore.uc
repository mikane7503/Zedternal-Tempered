class DKUpgrade_Skill_MoltenCore extends DKUpgrade_Skill
	config(ZedternalUnlimited);

// Molten Core: Increases magazine capacity and spare ammo reserves.
// Standard: +20% magazine size, +15% spare ammo.
// Deluxe:   +50% magazine size, +35% spare ammo.

var config array<float> MagCapacity;
var config array<float> SpareAmmo;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.MagCapacity[0] = 0.2f;
		default.MagCapacity[1] = 0.5f;
		default.SpareAmmo[0] = 0.15f;
		default.SpareAmmo[1] = 0.35f;

		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static simulated function ModifyMagSizeAndNumber(out int InMagazineCapacity, int DefaultMagazineCapacity, int upgLevel, KFWeapon KFW, optional array< class<KFPerk> > WeaponPerkClass, optional bool bSecondary=False, optional name WeaponClassname)
{
	InMagazineCapacity += Round(float(DefaultMagazineCapacity) * default.MagCapacity[upgLevel - 1]);
}

static simulated function ModifySpareAmmoAmount(out int InSpareAmmo, int DefaultSpareAmmo, int upgLevel, KFWeapon KFW, optional const out STraderItem TraderItem, optional bool bSecondary=False)
{
	InSpareAmmo += Round(float(DefaultSpareAmmo) * default.SpareAmmo[upgLevel - 1]);
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalRBPerkpackage.<langext>
    // Section: [DKUpgrade_Skill_MoltenCore]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalRBPerkpackage"
    LocSection="DKUpgrade_Skill_MoltenCore"

	UpgradeName="MoltenCore"
	upgradeDescription(0)="<font color=\"#42e087\">Increase</font> <font color=\"#42e087\">magazine capacity</font> by <font color=\"#84d6d0\">20%</font> and <font color=\"#42e087\">spare ammo</font> by <font color=\"#84d6d0\">15%</font>."
	upgradeDescription(1)="<font color=\"#42e087\">Increase</font> <font color=\"#42e087\">magazine capacity</font> by <font color=\"#84d6d0\">50%</font> and <font color=\"#42e087\">spare ammo</font> by <font color=\"#84d6d0\">35%</font>."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_MoltenCore'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_MoltenCore_Deluxe'

	Name="Default__DKUpgrade_Skill_MoltenCore"
}
