class ZTUpgrade_Perk_Voodoo extends ZTUpgrade_Perk config(ZedternalUnlimited);

var config float Health;
var config float Armor;
var config float Damage;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.Health = 0.05f;
		default.Armor = 0.05f;
		default.Damage = 0.35f;

		// BEGIN TEMPERED INI DEFAULTS (generated; do not edit by hand)
		default.Health = 0.050000f;
		default.Armor = 0.050000f;
		default.Damage = 0.050000f;
		// END TEMPERED INI DEFAULTS
		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static function ModifyHealth(out int InHealth, int DefaultHealth, int upgLevel)
{
	InHealth -= Round(float(DefaultHealth) * FMin(default.Health * upgLevel, 0.5f));
}

static function ModifyArmor(out int MaxArmor, int DefaultArmor, int upgLevel)
{
	MaxArmor -= Round(float(DefaultArmor) * FMin(default.Armor * upgLevel, 0.5f));
}

static function ModifyDamageGiven(out int InDamage, int DefaultDamage, int upgLevel, optional Actor DamageCauser, optional KFPawn_Monster MyKFPM, optional KFPlayerController DamageInstigator, optional Class<KFDamageType> DamageType, optional int HitZoneIdx, optional KFWeapon MyKFW)
    {
    InDamage += Round((float(DefaultDamage) * default.Damage) * float(upgLevel));
    }

defaultproperties
{
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Perks.UI_Perk_Voodoo_Rank_0'
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalTempered.<langext>
    // Section: [ZTUpgrade_Perk_Voodoo]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalTempered"
    LocSection="ZTUpgrade_Perk_Voodoo"
    LocalizeDescriptionLineCount=3

    UpgradeName="Voodoo"
	upgradeDescription(0)="<font color=\"#962ab9\">Cursed Vessel:</font> <font color=\"#cb1b43\">-%x%%</font> <font color=\"#cb1b43\">Maximum Health</font> (capped at <font color=\"#FFFFFF\">50%</font>)"
	upgradeDescription(1)="<font color=\"#962ab9\">Cursed Vessel:</font> <font color=\"#cb1b43\">-%x%%</font> <font color=\"#cb1b43\">Maximum Armor</font> (capped at <font color=\"#FFFFFF\">50%</font>)"
	upgradeDescription(2)="<font color=\"#962ab9\">Blood Pact:</font> <font color=\"#FFFFFF\">+%x%%</font> <font color=\"#42e087\">Damage</font>"
	PerkBonus(0)=(baseValue=0, incValue=5, maxValue=-1)
	PerkBonus(1)=(baseValue=0, incValue=5, maxValue=-1)
	PerkBonus(2)=(baseValue=0, incValue=5, maxValue=-1)
	// Legacy (hand-made) artwork icons

	Name="Default__ZTUpgrade_Perk_Voodoo"
}