class ZTWeaponUpg_TurretVision extends ZTUpgrade_Weapon abstract config(ZedternalUnlimited);

var config float DamageBonus;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 2)
	{
		default.DamageBonus = 0.3f;
		// BEGIN TEMPERED INI DEFAULTS (generated; do not edit by hand)
		default.DamageBonus = 0.300000f;
		// END TEMPERED INI DEFAULTS
		default.MODEVERSION = 2;
		static.StaticSaveConfig();
	}
}

static function bool IsUpgradeCompatible(class<KFWeapon> KFW)
{
	if (class<KFWeap_AutoTurret>(KFW) != None)
		return True;
	if (class<KFWeap_HRG_Warthog>(KFW) != None)
		return True;
	if (WMTurret_Interface(KFW) != None)
		return True;
	return False;
}

static function ModifyDamageGiven(out int InDamage, int DefaultDamage, int upgLevel, optional Actor DamageCauser, optional KFPawn_Monster MyKFPM, optional KFPlayerController DamageInstigator, optional class<KFDamageType> DamageType, optional int HitZoneIdx, optional KFWeapon MyKFW)
{
	if (MyKFPM != None && KFPawn_ZedStalker(MyKFPM) != None)
		InDamage += Round(float(DefaultDamage) * default.DamageBonus * upgLevel);
}

static simulated function bool CanSeeCloaked(int upgLevel, KFWeapon KFW, KFPawn OwnerPawn)
{
	return True;
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalTempered.<langext>
    // Section: [ZTWeaponUpg_TurretVision]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalTempered"
    LocSection="ZTWeaponUpg_TurretVision"

	UpgradeName="Thermal Optics"
	UpgradeDescription(0)="Your <font color=\"#15d7fa\">turrets</font> deal <font color=\"#77d914\">+%x%%</font> damage to <font color=\"#15d7fa\">Stalkers</font> per level and can detect <font color=\"#15d7fa\">cloaked enemies</font>."
	WeaponBonus=(baseValue=0, incValue=30, maxValue=-1)
	Name="Default__ZTWeaponUpg_TurretVision"
}
