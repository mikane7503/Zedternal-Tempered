class ZTUpgrade_Skill_BulletHell extends ZTUpgrade_Skill config(ZedternalUnlimited);

var config array<float> MagCapacity;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.MagCapacity[0] = 0.5f;
		default.MagCapacity[1] = 1.0f;

		// BEGIN TEMPERED INI DEFAULTS (generated; do not edit by hand)
		default.MagCapacity.Length = 2;
		default.MagCapacity[0] = 0.250000f;
		default.MagCapacity[1] = 0.500000f;
		// END TEMPERED INI DEFAULTS
		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static simulated function ModifyMagSizeAndNumber(out int InMagazineCapacity, int DefaultMagazineCapacity, int upgLevel, KFWeapon KFW, optional array< class<KFPerk> > WeaponPerkClass, optional bool bSecondary=False, optional name WeaponClassname)
{
	if (upgLevel > 0 && upgLevel <= default.MagCapacity.Length)
		InMagazineCapacity += Round(float(DefaultMagazineCapacity) * default.MagCapacity[upgLevel - 1]);
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalTempered.<langext>
    // Section: [ZTUpgrade_Skill_BulletHell]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalTempered"
    LocSection="ZTUpgrade_Skill_BulletHell"

    UpgradeName="Bullet Hell"
	upgradeDescription(0)="Increase <font color=\"#ff3399\">Magazine Capacity</font> by <font color=\"#66cc00\">25%</font>."
	upgradeDescription(1)="Increase <font color=\"#ff3399\">Magazine Capacity</font> by <font color=\"#66cc00\">50%</font>."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_BulletHell'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_BulletHell_Deluxe'
    Name="Default__ZTUpgrade_Skill_BulletHell"
}