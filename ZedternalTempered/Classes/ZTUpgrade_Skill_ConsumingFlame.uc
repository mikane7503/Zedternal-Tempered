class ZTUpgrade_Skill_ConsumingFlame extends ZTUpgrade_Skill config(ZedternalUnlimited);

var config array<int> Vampire;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		// Vampire is array<int> — must use integer literals here.
		// Previous values were 0.20f / 0.40f which truncate to 0,
		// causing the skill to heal for nothing.
		default.Vampire[0] = 20;
		default.Vampire[1] = 40;

		// BEGIN TEMPERED INI DEFAULTS (generated; do not edit by hand)
		default.Vampire.Length = 2;
		default.Vampire[0] = 20;
		default.Vampire[1] = 40;
		// END TEMPERED INI DEFAULTS
		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static function ModifyDamageGiven(out int InDamage, int DefaultDamage, int upgLevel, optional Actor DamageCauser, optional KFPawn_Monster MyKFPM, optional KFPlayerController DamageInstigator, optional class<KFDamageType> DamageType, optional int HitZoneIdx, optional KFWeapon MyKFW)
{
	if (MyKFPM != None && (MyKFPM.static.IsLargeZed() || MyKFPM.static.IsABoss()) && MyKFPM.IsAliveAndWell() && (MyKFPM.Health - InDamage) <= 0 && DamageInstigator != None && DamageInstigator.Pawn != None)
		DamageInstigator.Pawn.HealDamage(default.Vampire[upgLevel - 1], DamageInstigator, class'KFDT_Healing');
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalTempered.<langext>
    // Section: [ZTUpgrade_Skill_ConsumingFlame]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalTempered"
    LocSection="ZTUpgrade_Skill_ConsumingFlame"

    UpgradeName="Consuming Flame"
	upgradeDescription(0)="Killing <font color=\"e80948\">large ZEDs</font> heals you for <font color=\"#e85009\">20 Health</font>."
	upgradeDescription(1)="Killing <font color=\"e80948\">large ZEDs</font> heals you for <font color=\"#e85009\">40 Health</font>."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_ConsumingFlame'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_ConsumingFlame_Deluxe'

	Name="Default__ZTUpgrade_Skill_ConsumingFlame"
}
