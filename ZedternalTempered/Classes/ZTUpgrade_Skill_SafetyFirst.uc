class ZTUpgrade_Skill_SafetyFirst extends ZTUpgrade_Skill config(ZedternalUnlimited);

var config array<float> Resistance;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.Resistance[0] = 0.30f;
		default.Resistance[1] = 0.50f;
		// BEGIN TEMPERED INI DEFAULTS (generated; do not edit by hand)
		default.Resistance.Length = 2;
		default.Resistance[0] = 0.300000f;
		default.Resistance[1] = 0.500000f;
		// END TEMPERED INI DEFAULTS
		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}

static function ModifyDamageTaken(out int InDamage, int DefaultDamage, int upgLevel, KFPawn OwnerPawn, optional class<DamageType> DamageType, optional Controller InstigatedBy, optional KFWeapon MyKFW)
{
	if (ClassIsChildOf(DamageType, class'KFDT_Explosive') && KFPlayerController(InstigatedBy) != None)
		{
		InDamage = Round(float(InDamage) * (1.0f - default.Resistance[upgLevel - 1]));
        }
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalTempered.<langext>
    // Section: [ZTUpgrade_Skill_SafetyFirst]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalTempered"
    LocSection="ZTUpgrade_Skill_SafetyFirst"

    UpgradeName="Safety First"
	UpgradeDescription(0)="Increase resistance to <font color=\"#ff3399\">player explosives</font> by <font color=\"#66cc00\">30%</font>."
	UpgradeDescription(1)="Increase resistance to <font color=\"#ff3399\">player explosives</font> by <font color=\"#66cc00\">50%</font>."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_SafetyFirst'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_SafetyFirst_Deluxe'

	Name="Default__ZTUpgrade_Skill_SafetyFirst"
}
