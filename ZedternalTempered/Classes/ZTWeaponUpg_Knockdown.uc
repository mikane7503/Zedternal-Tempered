class ZTWeaponUpg_Knockdown extends ZTUpgrade_Weapon abstract config(ZedternalUnlimited);

var config float KnockdownBonus;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 2)
	{
		default.KnockdownBonus = 0.2f;
		// BEGIN TEMPERED INI DEFAULTS (generated; do not edit by hand)
		default.KnockdownBonus = 0.100000f;
		// END TEMPERED INI DEFAULTS
		default.MODEVERSION = 2;
		static.StaticSaveConfig();
	}
}

static function bool IsUpgradeCompatible(class<KFWeapon> KFW)
{
	return True;
}

static function ModifyKnockdownPower(out float InKnockdownPower, float DefaultKnockdownPower, int upgLevel, KFPawn OwnerPawn, optional class<DamageType> DamageType, optional byte BodyPart, optional bool bIsSprinting=False)
{
	InKnockdownPower += DefaultKnockdownPower * default.KnockdownBonus * upgLevel;
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalTempered.<langext>
    // Section: [ZTWeaponUpg_Knockdown]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalTempered"
    LocSection="ZTWeaponUpg_Knockdown"

	UpgradeName="Battering Ram"
	UpgradeDescription(0)="Increases your <font color=\"#15d7fa\">knockdown power</font> by <font color=\"#77d914\">+%x%%</font> per level."
	WeaponBonus=(baseValue=0, incValue=10, maxValue=-1)
	Name="Default__ZTWeaponUpg_Knockdown"
}
