class ZTUpgrade_Skill_ScorchedEarth extends ZTUpgrade_Skill config(ZedternalUnlimited);

// Scorched Earth: Enables ground fire and boosts ground fire damage.
// Standard: Ground fire active, +100% ground fire damage.
// Deluxe:   Ground fire active, +250% ground fire damage.

var config array<float> Damage;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.Damage[0] = 1.0f;
		default.Damage[1] = 2.5f;

		// BEGIN TEMPERED INI DEFAULTS (generated; do not edit by hand)
		default.Damage.Length = 2;
		default.Damage[0] = 0.500000f;
		default.Damage[1] = 1.000000f;
		// END TEMPERED INI DEFAULTS
		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static function ModifyDamageGiven(out int InDamage, int DefaultDamage, int upgLevel, optional Actor DamageCauser, optional KFPawn_Monster MyKFPM, optional KFPlayerController DamageInstigator, optional class<KFDamageType> DamageType, optional int HitZoneIdx, optional KFWeapon MyKFW)
{
	if (DamageType != None && ClassIsChildOf(DamageType, class'KFDT_Fire_Ground'))
		InDamage += Round(float(DefaultDamage) * default.Damage[upgLevel - 1]);
}

static simulated function bool IsGroundFireActive(int upgLevel, KFPawn OwnerPawn)
{
	return True;
}

static simulated function RevertUpgradeChanges(Pawn OwnerPawn)
{
	if (OwnerPawn != None && KFPlayerReplicationInfo(OwnerPawn.PlayerReplicationInfo) != None)
		KFPlayerReplicationInfo(OwnerPawn.PlayerReplicationInfo).bSplashActive = False;
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalTempered.<langext>
    // Section: [ZTUpgrade_Skill_ScorchedEarth]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalTempered"
    LocSection="ZTUpgrade_Skill_ScorchedEarth"

	UpgradeName="ScorchedEarth"
	upgradeDescription(0)="<font color=\"#42e087\">Enable</font> <font color=\"#42e087\">ground fire</font> and increase <font color=\"#42e087\">ground fire damage</font> by <font color=\"#84d6d0\">50%</font>."
	upgradeDescription(1)="<font color=\"#42e087\">Enable</font> <font color=\"#42e087\">ground fire</font> and increase <font color=\"#42e087\">ground fire damage</font> by <font color=\"#84d6d0\">100%</font>."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_ScorchedEarth'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_ScorchedEarth_Deluxe'

	Name="Default__ZTUpgrade_Skill_ScorchedEarth"
}
