class ZTWeaponUpg_MagSizeSmall extends ZTUpgrade_Weapon abstract config(ZedternalUnlimited);

var config int MagFlat;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 2)
	{
		default.MagFlat = 1;
		// BEGIN TEMPERED INI DEFAULTS (generated; do not edit by hand)
		default.MagFlat = 1;
		// END TEMPERED INI DEFAULTS
		default.MODEVERSION = 2;
		static.StaticSaveConfig();
	}
}

static function bool IsUpgradeCompatible(class<KFWeapon> KFW)
{
	if (KFW.default.MagazineCapacity[0] > 0 && KFW.default.MagazineCapacity[0] < 5)
		return True;
	return False;
}

static simulated function ModifyMagSizeAndNumber(out int InMagazineCapacity, int DefaultMagazineCapacity, int upgLevel, KFWeapon KFW, optional array< class<KFPerk> > WeaponPerkClass, optional bool bSecondary=False, optional name WeaponClassname)
{
	local int Val;
	Val = default.MagFlat;
	if (Val <= 0) Val = 1;
	InMagazineCapacity += Val * upgLevel;
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalTempered.<langext>
    // Section: [ZTWeaponUpg_MagSizeSmall]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalTempered"
    LocSection="ZTWeaponUpg_MagSizeSmall"

	UpgradeName="Compact Loader"
	UpgradeDescription(0)="Adds <font color=\"#77d914\">+%x</font> round to your <font color=\"#15d7fa\">magazine capacity</font> per level. (small-mag weapons)"
	WeaponBonus=(baseValue=0, incValue=1, maxValue=-1)
	Name="Default__ZTWeaponUpg_MagSizeSmall"
}
