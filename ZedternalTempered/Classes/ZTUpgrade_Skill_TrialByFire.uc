class ZTUpgrade_Skill_TrialByFire extends ZTUpgrade_Skill config(ZedternalUnlimited);

var config array<float> GrenadeDamage;
var config array<float> BossDamage;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.GrenadeDamage[0] = 0.30f;
		default.GrenadeDamage[1] = 0.60f;
		default.BossDamage[0] = 0.40f;
		default.BossDamage[1] = 0.60f;

		// BEGIN TEMPERED INI DEFAULTS (generated; do not edit by hand)
		default.GrenadeDamage.Length = 2;
		default.GrenadeDamage[0] = 0.300000f;
		default.GrenadeDamage[1] = 0.600000f;
		default.BossDamage.Length = 2;
		default.BossDamage[0] = 0.400000f;
		default.BossDamage[1] = 0.600000f;
		// END TEMPERED INI DEFAULTS
		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static function ModifyDamageGiven(out int InDamage, int DefaultDamage, int upgLevel, optional Actor DamageCauser, optional KFPawn_Monster MyKFPM, optional KFPlayerController DamageInstigator, optional class<KFDamageType> DamageType, optional int HitZoneIdx, optional KFWeapon MyKFW)
{
	if (DamageType != None && static.IsGrenadeDTAdvance(DamageType, DamageInstigator))
		InDamage += Round(float(DefaultDamage) * (default.GrenadeDamage[upgLevel - 1]));
	
    if((MyKFPM != none) && WMPawn_ZedPatriarch(MyKFPM) != none)
    {
        InDamage += Round(float(DefaultDamage) * (default.BossDamage[upgLevel - 1]));
    }
    else
    {
        // End:0xBE
        if((MyKFPM != none) && WMPawn_ZedFleshpoundKing(MyKFPM) != none)
        {
            InDamage += Round(float(DefaultDamage) * (default.BossDamage[upgLevel - 1]));
        }
        else
        {
            // End:0x11A
            if((MyKFPM != none) && WMPawn_ZedBloatKing(MyKFPM) != none)
            {
                    InDamage += Round(float(DefaultDamage) * (default.BossDamage[upgLevel - 1]));      
            }
            else
            {
                // End:0x173
                if((MyKFPM != none) && WMPawn_ZedHans(MyKFPM) != none)
                {
                    InDamage += Round(float(DefaultDamage) * (default.BossDamage[upgLevel - 1]));
                }
            }
        }
    }
}

defaultproperties
{
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalTempered.<langext>
    // Section: [ZTUpgrade_Skill_TrialByFire]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalTempered"
    LocSection="ZTUpgrade_Skill_TrialByFire"

    UpgradeName="Trial By Fire"
	upgradeDescription(0)="Increase your <font color=\"e80948\">Grenade Damage</font> by <font color=\"#e85009\">30%</font> and your <font color=\"e80948\">Damage to Bosses</font> by <font color=\"#e85009\">40%</font>."
	upgradeDescription(1)="Increase your <font color=\"e80948\">Grenade Damage</font> by <font color=\"#e85009\">60%</font> and your <font color=\"e80948\">Damage to Bosses</font> by <font color=\"#e85009\">60%</font>."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_TrialByFire'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_TrialByFire_Deluxe'

	Name="Default__ZTUpgrade_Skill_TrialByFire"
}