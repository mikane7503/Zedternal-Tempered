Class ZTUpgrade_Perk_Gambler extends ZTUpgrade_Perk
	config(ZedternalUnlimited);

var config int Doodle;
var config float Chance;
var config int Dosh;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.Doodle = 25;
		default.Chance = 0.01f;
		default.Dosh = 2;

		// BEGIN TEMPERED INI DEFAULTS (generated; do not edit by hand)
		default.Doodle = 40;
		default.Chance = 0.020000f;
		default.Dosh = 20;
		// END TEMPERED INI DEFAULTS
		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static function WaveEnd(int upgLevel, KFPlayerController KFPC)
{
    if(KFPC != none)
    {
        KFPlayerReplicationInfo(KFPC.PlayerReplicationInfo).AddDosh(default.Doodle * upgLevel);
    }
}

static function ModifyDamageGiven(out int InDamage, int DefaultDamage, int upgLevel, optional Actor DamageCauser, optional KFPawn_Monster MyKFPM, optional KFPlayerController DamageInstigator, optional class<KFDamageType> DamageType, optional int HitZoneIdx, optional KFWeapon MyKFW)
{
	if((HitZoneIdx == HZI_HEAD))
	{
	   if (FRand() <= (default.Chance * upgLevel))
		{
		KFPlayerReplicationInfo(DamageInstigator.PlayerReplicationInfo).AddDosh(1);
        }
     }
}


defaultproperties
{
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Perks.UI_Perk_Gambler_Rank_0'
    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalTempered.<langext>
    // Section: [ZTUpgrade_Perk_Gambler]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalTempered"
    LocSection="ZTUpgrade_Perk_Gambler"
    LocalizeDescriptionLineCount=2

    UpgradeName="Gambler"
	upgradeDescription(0)="You earn <font color=\"#77d914\">%x Dosh</font> at the end of every <font color=\"#15d7fa\">wave</font>."
	upgradeDescription(1)="You have a <font color=\"#77d914\">%x%%</font> chance to earn <font color=\"#77d914\">20 Dosh</font> on a <font color=\"#15d7fa\">Headshot kill</font>."
	PerkBonus(0)=(baseValue=0, incValue=40, maxValue=-1)
	PerkBonus(1)=(baseValue=0, incValue=2, maxValue=-1)
	// Legacy (hand-made) artwork icons

	Name="Default__ZTUpgrade_Perk_Gambler"
}