class ZTUpgrade_Skill_Crucible extends ZTUpgrade_Skill config(ZedternalUnlimited);

// Crucible: Increases fire damage over time scaling
// Normal: +150% fire DoT scaler | Deluxe: +400% fire DoT scaler

var config array<float> DamageOverTime;
var config int MODEVERSION;

static function UpdateConfig()
{
	if (default.MODEVERSION < 1)
	{
		default.DamageOverTime[0] = 1.5f;
		default.DamageOverTime[1] = 4.0f;

		// BEGIN TEMPERED INI DEFAULTS (generated; do not edit by hand)
		default.DamageOverTime.Length = 2;
		default.DamageOverTime[0] = 1.000000f;
		default.DamageOverTime[1] = 2.000000f;
		// END TEMPERED INI DEFAULTS
		default.MODEVERSION = 1;
		static.StaticSaveConfig();
	}
}


static function ModifyDoTScaler(out float InDoTScaler, float DefaultDotScaler, int upgLevel, optional class<KFDamageType> KFDT, optional bool bNapalmInfected)
{
	if (KFDT != None && ClassIsChildOf(KFDT, class'KFDT_Fire'))
		InDoTScaler += DefaultDotScaler * default.DamageOverTime[upgLevel - 1];
}

defaultproperties
{

    // --- Localization (auto-generated by mass_localize_dk_upgrades.py) ---
    // Strings live in KFGame/Localization/<lang>/ZedternalTempered.<langext>
    // Section: [ZTUpgrade_Skill_Crucible]  -- see DKUpgrade_* base class for details.
    bShouldLocalize=True
    LocPackage="ZedternalTempered"
    LocSection="ZTUpgrade_Skill_Crucible"

	UpgradeName="Crucible"
	UpgradeDescription(0)="Increase <font color=\"#e80948\">fire damage over time</font> scaling by <font color=\"#e85009\">100%</font>."
	UpgradeDescription(1)="Increase <font color=\"#e80948\">fire damage over time</font> scaling by <font color=\"#e85009\">200%</font>."
	UpgradeIcon(0)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_Crucible'
	UpgradeIcon(1)=Texture2D'ZedternalRBPerkpackage_Resources.Skills.UI_Skill_Crucible_Deluxe'

	Name="Default__ZTUpgrade_Skill_Crucible"
}
